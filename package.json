{
  "name": "pbrocks-block-starter",
  "version": "0.2.1",
  "description": "Blocks written with ESNext standard and JSX support – build step required.",
  "author": "pbrocks",
  "license": "GPL-2.0-or-later",
  "main": "build/index.js",
  "scripts": {
    "build": "export SET NODE_OPTIONS=--openssl-legacy-provider && wp-scripts build",
    "format": "wp-scripts format",
    "lint:css": "wp-scripts lint-style",
    "lint:js": "wp-scripts lint-js",
    "start": "wp-scripts start",
    "packages-update": "wp-scripts packages-update"
  },
  "dependencies": {
    "@wordpress/block-editor": "^8.0.13",
    "@wordpress/blocks": "^11.1.5",
    "@wordpress/i18n": "^4.2.4"
  },
  "devDependencies": {
    "@wordpress/scripts": "^19.2.2"
  }
}
